---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: workflows.engine.nagare.media
spec:
  group: engine.nagare.media
  names:
    categories:
    - nagare-all
    - nme-all
    - nagare
    - nme
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the workflows API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Specification of a Workflow.
            properties:
              config:
                description: Workflow configuration values.
                x-kubernetes-preserve-unknown-fields: true
              humanReadable:
                description: Human readable description of this Workflow.
                properties:
                  description:
                    description: Human readable description of this Workflow.
                    type: string
                  name:
                    description: Human readable name of this Workflow.
                    type: string
                type: object
              mediaLocations:
                description: Named references to MediaLocations.
                items:
                  properties:
                    name:
                      description: Name of the MediaLocation as used in the Workflow.
                      pattern: '[a-Z0-9-]+'
                      type: string
                    ref:
                      description: Reference to a MediaLocation of ClusterMediaLocation.
                        Only references to these two kinds are allowed. A Workflow
                        can only reference MediaLocations from its own Namespace.
                      properties:
                        apiVersion:
                          description: API version of the referred object.
                          type: string
                        kind:
                          description: Kind of the referred object.
                          type: string
                        name:
                          description: Name of the referred object.
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - name
                  - ref
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
          status:
            description: Status of a Workflow.
            properties:
              conditions:
                description: The latest available observations of an object's current
                  state. When a Workflow fails, one of the conditions will have type
                  "Failed" and status true. When a Workflow is completed, one of the
                  conditions will have type "Complete" and status true.
                items:
                  properties:
                    lastProbeTime:
                      description: Last time the condition was checked.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transit from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: (brief) reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of Workflow condition.
                      enum:
                      - Ready
                      - Complete
                      - Failed
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              endTime:
                description: Represents time when the Workflow has ended processing
                  (either failed or completed). It is not guaranteed to be set in
                  happens-before order across separate operations. It is represented
                  in RFC3339 form and is in UTC.
                format: date-time
                type: string
              startTime:
                description: Represents time when the Workflow controller started
                  processing a Workflow. It is represented in RFC3339 form and is
                  in UTC.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
